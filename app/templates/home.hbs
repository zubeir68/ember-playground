{{outlet}}
<div class="ui compact segment" style="margin-left: 650px">
    {{#ui-accordion class="styled"}}
    {{#each model as |book|}}
    <div class="title">
        {{book.title}}
    </div>
    <div class="content">
        Author: {{book.author}}
        <br>
        Release: {{book.year}}
    </div>
    {{/each}}
    {{/ui-accordion}}
</div>